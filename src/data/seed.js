// Sample data is intentionally empty — the app starts blank and everything
// is created by the user.
export const seedCategories = [];

export const seedProducts = [];

export const seedTransactions = [];
