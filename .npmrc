# Allow package install/build scripts to run.
# esbuild (used by Vite) needs its postinstall script to work, otherwise the
# dev server fails with "'vite' is not recognized" / esbuild errors.
allow-scripts=true
